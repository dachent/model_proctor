[
  {
    "id": "sf1_off_by_one",
    "category": "simple_fix",
    "set": "tune",
    "fixture": "sf1_off_by_one",
    "task_prompt": "The function sum_to_n in math_utils.py returns the wrong result for some inputs. Read the code, identify the bug, and fix it. Run: python check.py to verify your fix.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "invariants": [{"type": "file_contains", "path": "math_utils.py", "contains": "def sum_to_n"}],
    "timeout_seconds": 120,
    "initially_failing": true
  },
  {
    "id": "sf2_wrong_operator",
    "category": "simple_fix",
    "set": "holdout",
    "fixture": "sf2_wrong_operator",
    "task_prompt": "The function is_passing in grades.py returns incorrect results at the boundary. Read the code, find the bug, and fix it. Run: python check.py to verify your fix.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 120,
    "initially_failing": true
  },
  {
    "id": "sf3_missing_return",
    "category": "simple_fix",
    "set": "tune",
    "fixture": "sf3_missing_return",
    "task_prompt": "The function divide in calculator.py has a bug. Read the code carefully, find the issue, and fix it. Run: python check.py to verify your fix.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 120,
    "initially_failing": true
  },
  {
    "id": "sf4_string_method",
    "category": "simple_fix",
    "set": "holdout",
    "fixture": "sf4_string_method",
    "task_prompt": "The function capitalize_words in formatter.py produces wrong output. Read the code, find the bug, and fix it. Run: python check.py to verify your fix.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 120,
    "initially_failing": true
  },
  {
    "id": "ex1_call_chain",
    "category": "exploration",
    "set": "tune",
    "fixture": "ex1_call_chain",
    "task_prompt": "Explore this codebase and trace the complete call flow when process_request() is called from main.py. List every function called, in order, including the file name. Write your answer to ANSWER.md.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "ex2_module_structure",
    "category": "exploration",
    "set": "holdout",
    "fixture": "ex2_module_structure",
    "task_prompt": "Explore this codebase. Which module contains the data validation logic? List the module name and all validation-related function names. Write your answer to ANSWER.md.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "ex3_data_flow",
    "category": "exploration",
    "set": "tune",
    "fixture": "ex3_data_flow",
    "task_prompt": "Explore the data pipeline in this codebase. Describe how data flows through the system, naming each processing stage in order and what each stage does. Write your answer to ANSWER.md.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "ex4_error_handling",
    "category": "exploration",
    "set": "holdout",
    "fixture": "ex4_error_handling",
    "task_prompt": "Explore this codebase and describe the error handling patterns used. What exceptions are caught and how are they handled? Write your answer to ANSWER.md.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "me1_rename_func",
    "category": "mechanical",
    "set": "holdout",
    "fixture": "me1_rename_func",
    "task_prompt": "Rename the function 'get_data' to 'fetch_data' everywhere in the codebase — both the definition and all call sites. Do not change any logic. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "me2_convert_format",
    "category": "mechanical",
    "set": "holdout",
    "fixture": "me2_convert_format",
    "task_prompt": "Convert all percent-style string formatting to f-strings in all .py files. Do not change any logic or output. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "me3_add_type_hints",
    "category": "mechanical",
    "set": "tune",
    "fixture": "me3_add_type_hints",
    "task_prompt": "Add type hints (parameter types and return types) to all functions in all .py files. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "mf1_cache_layer",
    "category": "multifile",
    "set": "tune",
    "fixture": "mf1_cache_layer",
    "task_prompt": "Implement a simple in-memory cache class in cache.py with get(key) and set(key, value) methods. Then integrate it into data_loader.py so that load_data() caches its results. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "mf2_input_validation",
    "category": "multifile",
    "set": "holdout",
    "fixture": "mf2_input_validation",
    "task_prompt": "Create validation functions in validators.py and add input validation to all API functions in api.py. Invalid inputs should raise ValueError with a descriptive message. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "mf3_logging_system",
    "category": "multifile",
    "set": "holdout",
    "fixture": "mf3_logging_system",
    "task_prompt": "Implement a simple logging system in logger.py with log(level, message) and get_logs() functions. Add appropriate logging calls throughout service.py. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "mf4_plugin_system",
    "category": "multifile",
    "set": "holdout",
    "fixture": "mf4_plugin_system",
    "task_prompt": "Implement a plugin system in plugins.py with register_plugin(name, func) and run_plugin(name, *args) functions. Integrate it with app.py. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "db1_shared_state",
    "category": "debugging",
    "set": "tune",
    "fixture": "db1_shared_state",
    "task_prompt": "The function add_item in container.py has a bug where items leak between independent calls. Investigate, find the root cause, and fix it. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "db2_generator_state",
    "category": "debugging",
    "set": "holdout",
    "fixture": "db2_generator_state",
    "task_prompt": "The fibonacci generator in sequence.py produces incorrect results for some inputs. Find the root cause and fix it. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "db3_recursion_depth",
    "category": "debugging",
    "set": "holdout",
    "fixture": "db3_recursion_depth",
    "task_prompt": "The traverse function in tree.py crashes on certain tree structures. Find the root cause and fix it. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "mg1_config_migration",
    "category": "migration",
    "set": "tune",
    "fixture": "mg1_config_migration",
    "task_prompt": "The config system in config.py uses v1 format (flat keys). Migrate it to v2 format (nested structure). Write a migrate_v1_to_v2() function and a backward-compatibility shim so old code can still read v2 configs as v1. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "sec1_path_traversal",
    "category": "security",
    "set": "holdout",
    "fixture": "sec1_path_traversal",
    "task_prompt": "The read_file function in fileserver.py has a security vulnerability. Identify and fix it. Run: python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 180,
    "initially_failing": true
  },
  {
    "id": "big_explore",
    "category": "exploration",
    "set": "showcase",
    "fixture": "big_explore",
    "task_prompt": "Explore this codebase. A refund request enters via the HTTP layer. Trace the complete end-to-end call flow for a refund, in order, listing every module and function involved. Write your answer to ANSWER.md as a numbered list in the form `module.py::function_name`.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "invariants": [],
    "timeout_seconds": 900,
    "initially_failing": true
  },
  {
    "id": "bulk_migrate",
    "category": "mechanical",
    "set": "showcase",
    "fixture": "bulk_migrate",
    "task_prompt": "This project uses two deprecated patterns everywhere: `get_logger(__name__)` from deprecated_logging.py and imports from old_utils.py. Migrate every module to `Logger.get(__name__)` from logging_ext.py and to the utils/ package (utils.text.slugify, utils.num.clamp). The test suite must pass and no deprecated pattern may remain. Run python check.py to verify.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "invariants": [],
    "timeout_seconds": 900,
    "initially_failing": true
  },
  {
    "id": "q1_lru_cache",
    "category": "quality",
    "set": "v2",
    "fixture": "q1_lru_cache",
    "task_prompt": "Implement the LRUCache class in cache.py. LRUCache(capacity) creates a cache with the given capacity (capacity >= 1). get(key) returns the cached value, or -1 if the key is absent, and a successful get refreshes the entry's recency. put(key, value) inserts or updates an entry; put on an existing key updates the value and refreshes its recency. When the cache is full, put evicts the least-recently-used entry. Both get and put must run in O(1) average time. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q2_interval_merge",
    "category": "quality",
    "set": "v2",
    "fixture": "q2_interval_merge",
    "task_prompt": "Implement merge(intervals) in intervals.py. intervals is a list of [a, b] closed intervals with a <= b, and the input may be unsorted. Merge intervals that overlap OR touch: [1, 2] and [2, 3] merge to [1, 3]. Return the merged intervals as a list of [a, b] lists sorted by start. The input list must not be mutated. Empty input returns []. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q3_json_patch",
    "category": "quality",
    "set": "v2",
    "fixture": "q3_json_patch",
    "task_prompt": "Implement apply_patch(doc, ops) in patch.py, applying a restricted JSON Patch subset. ops is a list of operation dicts, each with keys op (one of add, remove, replace), path (an RFC 6901 JSON Pointer), and value (required for add and replace). Pointer segments unescape ~0 to ~ and ~1 to /. For arrays, the index - means append (valid for add only). remove of a non-existent path raises KeyError. apply_patch returns a NEW document and never mutates the input. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q4_retry_decorator",
    "category": "quality",
    "set": "v2",
    "fixture": "q4_retry_decorator",
    "task_prompt": "Implement retry(max_attempts=3, exceptions=(Exception,), backoff=0) in retry.py, a decorator factory. The wrapped function is called up to max_attempts times. Only exceptions that are instances of exceptions are retried; any other exception propagates immediately on first raise. After attempts are exhausted, the LAST raised exception is re-raised as the identical exception object. Before retry number i (1-based) it sleeps backoff * 2**(i-1) seconds. The wrapped function's __name__ and __doc__ are preserved. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q5_topk_stream",
    "category": "quality",
    "set": "v2",
    "fixture": "q5_topk_stream",
    "task_prompt": "Implement the TopK class in topk.py. TopK(k) tracks the k largest values added so far; k may be 0, in which case result() always returns []. add(x) adds a value. result() returns the k largest values added so far as a list sorted descending; equal values are ordered by insertion (earlier first). result() may be called any number of times interleaved with add() and must reflect all adds so far. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q6_config_precedence",
    "category": "quality",
    "set": "v2",
    "fixture": "q6_config_precedence",
    "task_prompt": "Implement load_config(defaults, file_cfg, env, cli) in config.py. All four arguments are dicts, and values in file_cfg, env and cli are strings. Precedence is cli > env > file_cfg > defaults. Any key in file_cfg, env or cli that is NOT present in defaults raises ValueError. String values are coerced to the type of the corresponding default, supporting str, int and bool: bool accepts true/false case-insensitive, and int is parsed from decimal strings. Return the merged dict. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q7_dag_topo",
    "category": "quality",
    "set": "v2",
    "fixture": "q7_dag_topo",
    "task_prompt": "Implement topo_sort(nodes, edges) in dag.py. nodes is a list of names and edges is a list of (from, to) tuples. Return a valid topological ordering of all nodes. When several nodes are simultaneously available, choose the lexicographically smallest name first, so the output is fully deterministic. A cycle raises ValueError. An edge referencing an unknown node raises ValueError. Empty nodes returns []. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q8_text_justify",
    "category": "quality",
    "set": "v2",
    "fixture": "q8_text_justify",
    "task_prompt": "Implement justify(words, width) in justify.py, returning a list of strings. Pack as many words per line as possible, greedily. For non-last lines with more than one word, distribute spaces so gaps differ by at most one space and left gaps receive the extra space. Every returned line is exactly width characters long. A line containing a single word is left-justified (padded on the right). The last line is left-justified with single spaces between words. A word longer than width raises ValueError. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q9_rate_limiter",
    "category": "quality",
    "set": "v2",
    "fixture": "q9_rate_limiter",
    "task_prompt": "Implement the RateLimiter class in limiter.py. RateLimiter(limit, window_seconds) allows at most limit calls per sliding window of window_seconds seconds. allow(t) takes a float timestamp t in seconds: it returns True and records the call when fewer than limit calls were allowed within the sliding interval (t - window_seconds, t]; a call exactly at t - window_seconds counts as expired. Otherwise it returns False. Only allowed calls are recorded. Calls may arrive with any t, not necessarily non-decreasing. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q10_parse_expr",
    "category": "quality",
    "set": "v2",
    "fixture": "q10_parse_expr",
    "task_prompt": "Implement evaluate(expr) in expr.py, evaluating an arithmetic expression string. Support + - * / with standard precedence, parentheses, unary minus, and arbitrary whitespace. Return the result as a float. Division by zero raises ZeroDivisionError. A malformed expression raises ValueError. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 240,
    "initially_failing": true
  },
  {
    "id": "q11_version_resolver",
    "category": "quality",
    "set": "v3",
    "fixture": "q11_version_resolver",
    "task_prompt": "Implement resolve(requirements, available) in resolver.py. requirements is a dict mapping package name to a constraint string; available maps package name to a list of version strings. A constraint string is a comma-separated list of clauses; each clause is one of the operators ==, >=, < or ~= followed by a version. ~= is the compatible-release operator: ~=X.Y means >=X.Y, <(X+1).0 and ~=X.Y.Z means >=X.Y.Z, <X.(Y+1).0. All clauses of a constraint must hold for a version to satisfy it. Versions are dot-separated non-negative integers compared numerically component by component (so 1.10 > 1.9); when two versions have different numbers of components the shorter one is padded with zeros (so 1.2 == 1.2.0). Resolution chooses the highest available version of each package that satisfies its constraint. If any package has no satisfying version (including a required package absent from available), raise ValueError naming the package. The result is a dict mapping every required package to its chosen version string; every required package must appear in the result. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q12_streaming_median",
    "category": "quality",
    "set": "v3",
    "fixture": "q12_streaming_median",
    "task_prompt": "Implement the MedianTracker class in median.py. MedianTracker() creates an empty tracker. add(x) adds one numeric value; duplicate values are supported. remove(x) removes ONE instance of x and raises ValueError if x is not present. median() returns the current median; when the count of values is even it returns the LOWER of the two middle values; it raises ValueError when the tracker is empty. add, remove and median may be interleaved in any order. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q13_mini_interpreter",
    "category": "quality",
    "set": "v3",
    "fixture": "q13_mini_interpreter",
    "task_prompt": "Implement run(program) in interp.py, an interpreter for a tiny language. program is a string of lines; each line is either an assignment '<name> = <expr>' or a print statement 'print <expr>' (a line whose first token is print). Blank lines and lines whose first non-space character is # are ignored. Expressions support integer literals, variable names (matching [A-Za-z_][A-Za-z0-9_]*), the binary operators + - * / with standard precedence (* and / bind tighter than + and -), parentheses, and unary minus. / is integer division truncated toward zero, so -7 / 2 == -3 and 7 / -2 == -3. Using an undefined variable raises NameError. Division by zero raises ZeroDivisionError. A line that is neither a valid assignment nor a valid print statement, or an expression with malformed syntax, raises ValueError. run returns the list of printed values as strings, in the order printed. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q14_diff_apply",
    "category": "quality",
    "set": "v3",
    "fixture": "q14_diff_apply",
    "task_prompt": "Implement apply_patch(text, patch) in diff.py. text is split into lines on '\\n' and the result is the lines joined on '\\n'. patch consists of one or more hunks; a hunk starts with a header line '@@ <start> <count> @@' where start is a 1-based line number in the ORIGINAL text and count is the number of original lines the hunk covers. In a hunk body (all lines until the next header or end of patch), lines starting with '-' delete an original line, lines starting with '+' insert a new line, and lines starting with a single space are context lines; any other body line is malformed and raises ValueError. The context and deletion lines of a hunk, in order, must exactly equal the count original lines starting at start; any mismatch or a wrong number of consumed lines raises ValueError. A hunk replaces its original slice with the kept context and inserted lines, in order; with count 0 the '+' lines are inserted immediately before original line start (start may be one past the last line to append at the end). Hunks are given in ascending start order and never overlap; line numbers always refer to the ORIGINAL text, so earlier hunks must not shift where later hunks apply. apply_patch returns the patched string. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q15_lru_ttl",
    "category": "quality",
    "set": "v3",
    "fixture": "q15_lru_ttl",
    "task_prompt": "Implement the LRUCache class in cache.py: LRUCache(capacity, ttl_seconds) with capacity >= 1 and ttl_seconds > 0. get(key, now=0.0) and put(key, value, now=0.0) take an optional float timestamp now in seconds (0.0 when omitted). An entry put at time t is live while now < t + ttl_seconds and is expired when now >= t + ttl_seconds; the TTL is anchored to the LAST put of that key. get does NOT extend the TTL, but a successful get DOES refresh the entry's LRU recency. get of an expired or missing key returns -1. Expired entries are treated as absent for capacity purposes: they are removed lazily and never force an eviction. When a put of a new key would exceed capacity, the least-recently-used LIVE entry is evicted. put on an existing key updates the value, refreshes recency, and re-anchors the TTL to the new put time. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q16_calendar_scheduler",
    "category": "quality",
    "set": "v3",
    "fixture": "q16_calendar_scheduler",
    "task_prompt": "Implement schedule(events, day_start, day_end) in cal.py. Each event is a dict with keys name (str), duration (int minutes, > 0), priority (int; LOWER means more important), and not_before (int minutes since midnight). Place events greedily in priority order; break ties by earlier not_before, then by name in alphabetical order. Each event goes in its earliest fitting slot: the earliest start >= max(not_before, day_start) such that [start, start + duration) does not overlap any already-placed event and start + duration <= day_end. Back-to-back packing is allowed: an event may start exactly when another ends (end == next start is not an overlap). Events that cannot fit are dropped silently. Return a list of (name, start, end) tuples sorted by start. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q17_json_diff",
    "category": "quality",
    "set": "v3",
    "fixture": "q17_json_diff",
    "task_prompt": "Implement diff(a, b) and apply_diff(a, ops) in jdiff.py for JSON-like structures (dicts, lists, strings, numbers, booleans, None). diff returns a list of ops, each a dict {\"op\": \"set\", \"path\": [...], \"value\": v} or {\"op\": \"delete\", \"path\": [...]}, where a path is a list of dict keys (strings) and list indices (ints). Dicts recurse: keys in both are recursed into, keys only in b produce set ops, keys only in a produce delete ops. Lists are compared element-wise by index: index i of a is diffed against index i of b; if b has extra elements they are added with set ops whose final path component is an index equal to the list length at apply time (a set at index == len(list) appends); if a has extra elements they are removed with delete ops emitted from the highest index downward. Scalars, and any value whose type differs from its counterpart, produce a single set op at that path when the values are not equal. Ops are emitted so that applying them in order transforms a into b, with dict keys processed in sorted order. diff returns [] when a == b. apply_diff(a, ops) returns the patched structure and must NOT mutate a; a delete op removes the dict key or list element at its path; apply_diff(a, diff(a, b)) == b must hold. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q18_template_engine",
    "category": "quality",
    "set": "v3",
    "fixture": "q18_template_engine",
    "task_prompt": "Implement render(template, context) in tmpl.py. Literal text passes through unchanged. {{ name }} substitutes a value from context; whitespace inside the braces is ignored; dotted names do nested lookup ({{ user.name }} looks up context[\"user\"][\"name\"]); names are dot-separated identifiers. Substituted values are HTML-escaped: & -> &amp;, < -> &lt;, > -> &gt;, \" -> &quot;, ' -> &#x27; (& is escaped first); non-string values are converted with str() before escaping. {{{ name }}} substitutes the value WITHOUT escaping, with the same lookup rules. A missing key at any point of the lookup raises KeyError. A {{ or {{{ tag that is never closed raises ValueError; an empty or malformed name inside braces raises ValueError. Single braces and any other text are literal. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q19_token_bucket",
    "category": "quality",
    "set": "v3",
    "fixture": "q19_token_bucket",
    "task_prompt": "Implement the TokenBucket class in bucket.py: TokenBucket(capacity, refill_per_second) with capacity > 0 and refill_per_second >= 0. The bucket starts FULL (it holds capacity tokens at time 0). Tokens refill continuously at refill_per_second tokens per second, up to but never above capacity. allow(t, cost=1.0) takes a float time t in seconds (calls arrive with non-decreasing t) and a possibly fractional cost. Whether or not the call is allowed, the stored level is first refilled for the elapsed time since the previous call: level = min(capacity, level + (t - t_prev) * refill_per_second). Then, if level >= cost, allow returns True and deducts cost; otherwise it returns False and deducts nothing. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  },
  {
    "id": "q20_csv_parse",
    "category": "quality",
    "set": "v3",
    "fixture": "q20_csv_parse",
    "task_prompt": "Implement parse(text) in csvparse.py, an RFC-4180-ish CSV parser returning a list of rows, each a list of field strings. Fields are separated by commas and rows by newlines. CRLF line endings are normalized: every '\\r\\n' is treated as '\\n' before parsing (a lone '\\r' elsewhere is an ordinary character). A trailing newline does NOT create an empty final row, so \"a\\n\" and \"a\" both parse to [[\"a\"]]; a blank line in the middle is a row containing one empty field. Empty input returns []. A field wrapped in double quotes may contain commas, newlines, and literal double quotes escaped as \"\" (two double-quote characters). A quoted field's closing quote must be immediately followed by a comma, a newline, or end of input; anything else raises ValueError. A quoted field that is never closed raises ValueError. Unquoted fields are taken verbatim: no trimming of whitespace, and a double quote inside an unquoted field is an ordinary character. Run: python check.py to verify your work.",
    "acceptance": {"command": "python check.py", "expect_exit": 0},
    "hidden_test": {"command": "python hidden_check.py", "expect_exit": 0},
    "timeout_seconds": 360,
    "initially_failing": true
  }
]
