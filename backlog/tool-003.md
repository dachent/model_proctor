## Stable key
TOOL-003

## Outcome
The F9 session-economics rule (new leader session only when steady context C > ~3-5x resume canon S) is validated against measured `session_rebuild` token classes, and the break-even formula constants are replaced with measured values in STATIC_CASCADE_SPEC §11 (currently model-derived estimates).

## Why
policy/leader-context-and-worker-integrity.md F9 requires savings to be MEASURED before adoption claims. meter.py has the session_rebuild token class; nobody has collected the data.

## In scope
- Collect session_rebuild + steady-context measurements across >= 5 real goals.
- Update spec §11 with measured constants or flag the model as wrong.

## Blocked by
None

## Final evidence and handoff
(to be filled at close)
