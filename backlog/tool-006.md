## Stable key
TOOL-006

## Status
PARKED. Activation trigger: the subprocess envelope is proven insufficient for a real need — specifically streaming partial results, mid-run cancellation semantics, or bidirectional tool-call loops that one-shot subprocess execution cannot express. Until then, a service transport is complexity without evidence (the Phase-1 plan's core surviving decision was to NOT impersonate a model endpoint).

## Outcome (if activated)
Evaluate ACP/MCP/HTTP transport for delegate behind the stable wrapper interface, per the reverse-engineered scaffold's transport-independence constraint.

## Blocked by
None (activation-triggered)

## Final evidence and handoff
(parked — no close planned)
