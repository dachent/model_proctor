## Stable key
TOOL-007

## Status
PARKED. Activation trigger: TOOL-004 (eval v2) shows the static plan-time allocation leaves > 2x cost on the table vs per-task oracle routing — i.e. misclassification, not the static design, dominates. The Phase-0 adversarial review explicitly listed per-repo auto-benchmarking and learned routers as NOT established in the reference product; do not rebuild the rejected dynamic design on a hunch.

## Outcome (if activated)
Scoped investigation of per-repo difficulty priors from eval history.

## Blocked by
- #4 (TOOL-004) (as activation evidence, not as work dependency)

## Final evidence and handoff
(parked — no close planned)
